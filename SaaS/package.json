{
  "name": "mindbridge-saas",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @mindbridge/api & npm run dev --workspace @mindbridge/web",
    "build": "npm run build --workspace @mindbridge/shared && npm run build --workspace @mindbridge/api && npm run build --workspace @mindbridge/web",
    "lint": "npm run lint --workspace @mindbridge/api && npm run lint --workspace @mindbridge/web",
    "db:generate": "npm run db:generate --workspace @mindbridge/api",
    "db:migrate": "npm run db:migrate --workspace @mindbridge/api",
    "db:seed": "npm run db:seed --workspace @mindbridge/api"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
